// Lightbox Configuration
lightbox.option({
    'resizeDuration': 200, // Animation duration when resizing the image
    'wrapAround': true, // Allow cycling through images
    'showImageNumberLabel': false, // Hide image number label
    'alwaysShowNavOnTouchDevices': true, // Show navigation arrows on touch devices
    'disableScrolling': true, // Prevent page scrolling when lightbox is open
    'fadeDuration': 200, // Fade-in duration
    'positionFromTop': 50 // Distance from the top of the viewport
});